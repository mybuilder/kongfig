sudo: required
services:
  - docker
language: node_js
node_js:
    - "stable"
    - "4"
    - "6"
before_install:
    -  docker run -d --name kong-database -e "POSTGRES_USER=kong" -e "POSTGRES_DB=kong"  postgres:9.4
    -  sleep 5
    -  docker run -d --name kong --link kong-database:kong-database -e "KONG_DATABASE=postgres" -e "KONG_PG_HOST=kong-database" -p 127.0.0.1:8001:8001 -p 127.0.0.1:8000:8000 kong:latest
    -  sleep 5
    -  curl localhost:8001
